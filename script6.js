/*
VALUE AND TYPE

a = 42;
a = String(a); // take the value of a, turn it into a string EXPLICITLY

console.log(a); // returns black number (meaning its a string)
console.log("hello world") // returns black text (cuz its also a string)

b = Number(a); // returns blue string (meaning its a number)

In JS, there's no such thing as a variable having a type.
In JS, the value has the type, 

IMPLICIT COERSIONS

a = 42;
a = a + "";
console.log(a); // returns black


*/